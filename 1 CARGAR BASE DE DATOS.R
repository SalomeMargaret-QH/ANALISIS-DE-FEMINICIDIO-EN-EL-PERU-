# Establecer el directorio de trabajo (opcional)
setwd("C:/Users/SALOME/Documents/1 UNIVERSIDAD SALOME")
datos <- read.csv("casos_feminicidio.csv", encoding = "UTF-8")

# Ver las primeras filas para confirmar
head(datos)

# Ver los nombres de las columnas
names(datos)

